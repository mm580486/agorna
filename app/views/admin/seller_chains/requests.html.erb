

<%# content_for :head do %>
  
  
<%# end %>
<main>

 <div class="row">
      <div class="col s12 m12">
          
        <div class="card-panel white">

        <table class='striped highlight bordered'>
            <thead>
              <tr>
                  
                 <th>وضعیت</th>
                  <th>لینک</th>
  <th>در خواست </th>
              </tr>
            </thead>
    
            <tbody>
            <% @chains.each do |chain| %>
              <tr>
                <td>
                  <%= chain.accept? ? 'تایید شده':'تایید نشده' %>
                </td>
                <td><%= link_to User.find(chain.user_two).exposition_name,exposition_show_path(User.find(chain.user_two).identify),terget: '_blank' %></td>
                <td><%= User.find(chain.user_two).name %></td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
 </div>
 <%= link_to(raw('<i class="material-icons">add</i>'),new_admin_seller_chain_path,class: 'btn-floating btn-large waves-effect waves-light red',style: 'position:fixed;bottom: 20px; left: 24px;') %>
</main>